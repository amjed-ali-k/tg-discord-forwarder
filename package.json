{
  "name": "node-project-template",
  "version": "1.3.0",
  "scripts": {
    "lint:check": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "echo 'TODO: Add a test runner and then update this command'",
    "serve": "nodemon ./src/index.ts --trace-warnings",
    "start": "pnpm serve",
    "build": "mkdir -p build && cp src/*.js build # TODO: Replace with your build command",
    "preversion": "git fetch --tags && pnpm lint:check && npm test",
    "version": "pnpm build",
    "postversion": "git push --follow-tags"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "eslint": "^7.7.0",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "engines": {
    "node": ">=14"
  },
  "displayName": "Telegram to Discord",
  "description": "Template for creating Node.js projects including docs and a deploy pipeline",
  "author": "Amjed Ali K",
  "license": "MIT",
  "keywords": [
    "template",
    "skeleton",
    "quickstart"
  ],
  "dependencies": {
    "deta": "^1.1.0",
    "discord.js": "^14.7.0",
    "dotenv": "^16.0.3",
    "input": "^1.0.1",
    "telegram": "^2.14.7"
  }
}
